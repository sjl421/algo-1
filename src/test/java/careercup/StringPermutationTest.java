package careercup;

import org.junit.Test;

import static org.hamcrest.core.Is.is;
import static org.junit.Assert.*;
import java.util.*;

/**
 * @author: Artur Khalikov
 */
public class StringPermutationTest {

    @Test
    public void testFind1() throws Exception {
        /*List<String> p = StringPermutation.find("a");
        assertArrayEquals(new String[]{"a"}, p.toArray());

        p = StringPermutation.find("ab");
        assertArrayEquals(new String[]{"ab", "ba"}, p.toArray());

        p = StringPermutation.find("abc");
        assertArrayEquals(new String[]{"abc", "bac", "cab", "cba", "acb", "bca"}, p.toArray());*/
    }
}
