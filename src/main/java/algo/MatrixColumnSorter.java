package algo;

/**
 * You have a numeric (m, n) matrix.
 * We will call sum of modules of negative elements in a column as column characteristic.
 *
 * Input: two integer numbers m, n
 * Randomly generated numeric matrix of size (m, n).
 *
 * Output: matrix sorted in ascending order of column characteristic.
 *
 * @author ahalikov
 */
public class MatrixColumnSorter {

    static int[][] sort(int[][] matrix) {
        return matrix;
    }
}
